name: game-hall

x-default-logging: &default-logging
  driver: json-file
  options:
    max-size: "20m"
    max-file: "5"

services:
  mysql:
    image: mysql:8.4
    container_name: game-hall-mysql
    restart: unless-stopped
    logging: *default-logging
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:-game_hall}
      MYSQL_USER: ${MYSQL_USER:-game_hall}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?Set MYSQL_PASSWORD in .env}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?Set MYSQL_ROOT_PASSWORD in .env}
      TZ: Asia/Shanghai
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_0900_ai_ci
    ports:
      - "${MYSQL_PUBLISH_HOST:-127.0.0.1}:${MYSQL_PUBLISH_PORT:-1025}:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test:
        - CMD-SHELL
        - mysqladmin ping -h 127.0.0.1 -u root -p"$${MYSQL_ROOT_PASSWORD}" --silent
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  redis:
    image: redis:7.4-alpine
    container_name: game-hall-redis
    restart: unless-stopped
    logging: *default-logging
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?Set REDIS_PASSWORD in .env}
      TZ: Asia/Shanghai
    command:
      - sh
      - -c
      - exec redis-server --appendonly yes --requirepass "$${REDIS_PASSWORD}"
    ports:
      - "${REDIS_PUBLISH_HOST:-127.0.0.1}:${REDIS_PUBLISH_PORT:-7878}:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test:
        - CMD-SHELL
        - redis-cli -a "$${REDIS_PASSWORD}" ping | grep -q PONG
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s

  migrate:
    image: game-hall-app:local
    logging: *default-logging
    command:
      - alembic
      - -c
      - backend/alembic.ini
      - upgrade
      - head
    environment:
      DATABASE_URL: mysql+pymysql://${MYSQL_USER:-game_hall}:${MYSQL_PASSWORD:?Set MYSQL_PASSWORD in .env}@mysql:3306/${MYSQL_DATABASE:-game_hall}?charset=utf8mb4
    depends_on:
      mysql:
        condition: service_healthy
    restart: "no"

  app:
    image: game-hall-app:local
    build:
      context: .
      args:
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.org/simple}
    container_name: game-hall-app
    restart: unless-stopped
    logging: *default-logging
    environment:
      DATABASE_URL: mysql+pymysql://${MYSQL_USER:-game_hall}:${MYSQL_PASSWORD:?Set MYSQL_PASSWORD in .env}@mysql:3306/${MYSQL_DATABASE:-game_hall}?charset=utf8mb4
      REDIS_URL: redis://:${REDIS_PASSWORD:?Set REDIS_PASSWORD in .env}@redis:6379/0
      LOG_DIR: /var/log/game-hall
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      LOG_RETENTION_DAYS: ${LOG_RETENTION_DAYS:-30}
      GAME_HALL_SIGNING_SECRET: ${GAME_HALL_SIGNING_SECRET:-game-hall-local-session-v1}
      PIKAFISH_THREADS: ${PIKAFISH_THREADS:-1}
      PIKAFISH_HASH_MB: ${PIKAFISH_HASH_MB:-64}
      TZ: ${TZ:-Asia/Shanghai}
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    ports:
      - "${GAME_HALL_PORT:-${AVALON_PORT:-10618}}:8000"
    volumes:
      - ./logs:/var/log/game-hall

volumes:
  mysql-data:
  redis-data:
