{
  "name": "game-hall",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n SERVER,WEB -c magenta,cyan \"npm run dev:server\" \"npm run dev:web\"",
    "dev:server": ".venv/bin/python -m uvicorn backend.app.main:app --env-file .env --reload --host 0.0.0.0 --port 10618",
    "dev:web": "npm --prefix frontend run dev -- --host 0.0.0.0",
    "build": "npm --prefix frontend run build",
    "test": "(cd backend && ../.venv/bin/python -m pytest) && npm --prefix frontend run test:run",
    "smoke": "node frontend/scripts/smoke.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
