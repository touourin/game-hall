import { createCompetitiveStatsPresentation } from '../../game-platform/recordFormatting'

export const doudizhuStats = createCompetitiveStatsPresentation({
  roleLabels: { landlord: '地主', farmer: '农民' },
})
