{
  "name": "game-hall-web",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "plugins:sync": "node scripts/generate-plugin-registry.mjs",
    "dev": "npm run plugins:sync && vite",
    "build": "npm run plugins:sync && vue-tsc -b && vite build && npm run verify:theme-selectors && npm run verify:theme-contrast",
    "verify:theme-selectors": "node scripts/verify-theme-selectors.mjs",
    "verify:theme-contrast": "node scripts/verify-theme-contrast.mjs",
    "test": "npm run plugins:sync && vitest",
    "test:run": "npm run plugins:sync && vitest run",
    "smoke:arcade": "node scripts/arcade-smoke.mjs"
  },
  "dependencies": {
    "@lucide/vue": "^1.27.0",
    "pinia": "^3.0.4",
    "qrcode.vue": "^3.6.0",
    "socket.io-client": "^4.8.3",
    "vue": "^3.5.24",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@vitejs/plugin-vue": "^6.0.3",
    "@vue/test-utils": "^2.4.6",
    "jsdom": "^27.2.0",
    "typescript": "~5.9.3",
    "vite": "^8.0.0",
    "vitest": "^4.0.15",
    "vue-tsc": "^3.1.5"
  }
}
